import { ScrollTrackDirective } from './scroll-track.directive';

describe('ScrollTrackDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollTrackDirective();
    expect(directive).toBeTruthy();
  });
});
