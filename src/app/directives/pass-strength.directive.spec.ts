import { PassStrengthDirective } from './pass-strength.directive';

describe('PassStrengthDirective', () => {
  it('should create an instance', () => {
    const directive = new PassStrengthDirective();
    expect(directive).toBeTruthy();
  });
});
